{
  "private": true,
  "scripts": {
    "build": "cd server && npm run start",
    "vercel-build": "pnpm install && pnpm build"
  },
  "dependencies": {
    "@vercel/node": "^3.2.26",
    "@vercel/static-build": "^2.5.35"
  }
}